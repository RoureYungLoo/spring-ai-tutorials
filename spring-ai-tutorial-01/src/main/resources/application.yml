spring:
  application:
    name: spring-ai-tutorial-01
  ai:
    ollama:
      chat:
        enabled: true
        model: deepseek-r1:7b
        options:
          temperature: 0.8
      # base-url: http://localhost:11434/
      # base-url: http://172.18.244.76:11434/
      base-url: http://localhost:11434/
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/spring-ai-ollama?useUnicode=true&characterEncoding=utf8&zeroDateTimeBehavior=convertToNull&useSSL=true&serverTimezone=GMT%2B8
    username: root
    password: o#DwN&JSa56
  data:
    redis:
      database: 0
      host: localhost
      port: 6379
      jedis:
        pool:
          enabled: true
          max-active: 8
          max-idle: 8
          min-idle: 0
          max-wait: -1ms

server:
  port: 8080

logging:
  level:
    com.luruoyang: debug
    org.springframework.ai: debug
